services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile

  web:
    build:
      context: apps/web
      dockerfile: Dockerfile
      args:
        LEXORA_API_URL: http://api:8011
