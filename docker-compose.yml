services:
  postgres:
    image: pgvector/pgvector:pg17
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lexora}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lexora}
      POSTGRES_USER: ${POSTGRES_USER:-lexora}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-lexora} -d ${POSTGRES_DB:-lexora}",
        ]
      interval: 5s
      timeout: 3s
      retries: 10
    ports:
      - "127.0.0.1:5434:5432"
    volumes:
      - lexora-postgres-data:/var/lib/postgresql/data

  api:
    image: lexora-api:local
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      additional_contexts:
        agent-platform: ../dayboard/packages/agent-platform
        rag-core: ../rag-core
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-lexora}:${POSTGRES_PASSWORD:-lexora}@postgres:5432/${POSTGRES_DB:-lexora}
      LEGAL_SOURCE_REPOSITORY_PATH: /data/lvyan-lawtext
      LEXORA_HOST: 0.0.0.0
      LEXORA_PORT: 8011
    command:
      - sh
      - -c
      - /app/.venv/bin/alembic upgrade head && exec /app/.venv/bin/lexora-api
    ports:
      - "127.0.0.1:8011:8011"
    volumes:
      - ../lvyan-lawtext:/data/lvyan-lawtext:ro
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "/app/.venv/bin/python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8011/api/v1/health', timeout=5).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  web:
    image: lexora-web:local
    build:
      context: apps/web
      dockerfile: Dockerfile
      args:
        LEXORA_API_URL: http://api:8011
    restart: unless-stopped
    environment:
      LEXORA_API_URL: http://api:8011
      NODE_ENV: production
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/').then(response => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s

volumes:
  lexora-postgres-data:
